# v0.3 Implementation Log

This is a human-readable checkpoint log for the v0.3 implementation. It records evidence and blockers; it is not a generated benchmark artifact.

## Phase 0 — Preflight

Status: complete

Date: 2026-07-13

Branch: codex/v0.3-late-antiquity

Evidence:

- Repository began on main with no tracked modifications.
- Existing untracked user-owned outputs were preserved:
  - outputs/ask_scriptorium_runs.md
  - outputs/chunk_embeddings_embeddinggemma.json
- v0.2 baseline passed: 18 tests, Python 3.13, 21 seconds.
- Privacy and portability check passed.
- Ruff is not available in the current environment; lint remains an explicit later environment gate.
- Existing v0.2 inputs, reports, and methodology were not modified.

Next permitted phase: Phase 1 — rights register and corpus-pack skeleton.

## Phase 1 — Rights Register and Corpus-Pack Skeleton

Status: complete

Evidence:

- Added sources_public/source_register.v2.json with nine approved-in-scope editions and one mechanically blocked Gregory of Nyssa candidate.
- Added data/packs/late_antiquity_core.v1.json with explicit active, blocked, and inactive candidate source IDs.
- Added source-register and pack validation with fail-closed checks for status, identity, portable paths, checksums, and pack membership.
- Added four source-register contract tests; all pass.
- No raw source text was activated or copied during this phase.

Next permitted phase: Phase 2 — versioned contracts and pack-aware CLI.

## Phase 2 — Versioned Contracts and Pack-Aware CLI

Status: complete

Evidence:

- Added artifact-specific source-register and question schema constants without changing v0.2 validators.
- Added passage-anchored v2 question validation for answerable, unanswerable, alternative-evidence, and required multi-hop groups.
- Added explicit corpus-pack resolution by declared pack ID.
- Added a pack validation CLI command that emits machine-readable JSON.
- Added 10 new contract and pack/CLI tests; the full suite now passes 28 tests.
- Full regression command: PYTHONPATH=src python3 -m unittest discover -s tests -q — 28 tests passed in 25 seconds.
- Privacy and portability check passed.
- Existing v0.2 ingest, retrieve, evaluate, report, and output behavior remains green.

Next permitted phase: Phase 3 — blinded review-classics protocol tracer.

## Phase 3 — Blinded review-classics protocol tracer

Status: complete

Evidence:

- Added the project-local `review-classics` skill with schema, rubric, independence, and staleness references.
- Added packet/result validators, deterministic export, reconciliation, and dependency-staleness helpers.
- Added CLI export and validation commands; targeted review and CLI tests pass.
- The official skill quick validator could not run because the environment lacks the optional `yaml` module; manual syntax checks and the project validators pass.

Next permitted phase: Phase 4 — two-author ingestion tracer.

## Phase 4 — Two-author ingestion tracer

Status: complete

Evidence:

- Acquired the approved Project Gutenberg Iamblichus text (#72815) into ignored raw-source cache; the page identifies it as the 1895 second-edition reprint of the unaltered 1821 text.
- Added deterministic v0.3 normalization, canonical passage, and source-bounded chunk builders without changing v0.2 scripts.
- Added `late-antiquity-tracer-v1` and made `scriptorium ingest --pack` rebuild its corpus offline from committed normalized texts.
- The tracer rebuilt 1,694 passages and 235 chunks across Boethius and Iamblichus.
- Added ingestion and CLI process tests; the full suite passes 39 tests.
- Privacy/portability and `git diff --check` pass.

Next permitted phase: Phase 5 — corpus expansion and runtime repair.

## Phase 5 — Corpus expansion and runtime repair

Status: complete

Evidence:

- Added three-source-capable deterministic ingestion path and the interactive grounded-evidence `ask` command.
- Added TTY-only Unicode block-art medieval figure support without contaminating JSON retrieval output.
- README now distinguishes the executable two-author tracer from the pending full pack.
- 40-test suite, privacy/portability, and diff checks pass.
- Initial expansion was held at the register boundary while direct Gutenberg endpoints failed; no substitute edition was activated.
- A consolidated acquisition pass recovered the remaining Gutenberg, Rolt, and original MacKenna inputs. The full nine-source pack now rebuilds 13,875 passages and 2,008 chunks.
- Added an offline three-configuration chunker calibration report; it records outliers rather than selecting a held-out-tuned winner.
- Added deterministic oversized-passage fallback; the rebuilt eight-source corpus has 1,554 chunks with no chunk exceeding the configured 750-word ceiling.
- Acquired and verified original MacKenna volumes I–IV; the full nine-source pack now rebuilds 13,875 passages and 2,008 chunks with a 750-word maximum.

## Phase 6 — Calibration and chunker freeze

Status: complete

Evidence:

- Calibration report compares three paragraph-window configurations across all nine acquired sources without using held-out questions.
- The selected tracer configuration is `paragraph-window-v1` (target 500, maximum 750) for continued development; selection remains provisional until reviewed benchmark evidence exists.
- Canonical passage anchors remain independent of chunk configuration, and derived relevance is generated at evaluation time.
- The full nine-source pack uses the calibrated `paragraph-window-v1` configuration; no chunk exceeds the documented maximum.

Next permitted phase: complete benchmark review/freeze, then declare the chunker selection final.

## Phase 7 — Candidate benchmark and review

Status: complete

Evidence:

- Added the review policy artifact and deterministic candidate generator.
- Generated exactly 100 passage-anchored candidates for the acquired-source pack with the planned 30/20/10/20/10/10 type distribution and 70/30 development/test split.
- Candidate state is explicitly `candidate`; no item is presented as accepted or scholarly validated.
- Added derived chunk relevance and an offline v0.3 retrieval evaluation command. The current BM25 candidate run records 90 answerable items separately from 10 unanswerables.
- Added controlled `review accept` promotion: material disagreements remain candidates unless an explicit adjudication decision exists. A no-review acceptance attempt correctly exits non-zero with all 100 unresolved.
- Added a 100-item human-adjudication template with empty decisions; it preserves the boundary between engineering output and domain judgment.
- The full-source 100-item review packet validates successfully; review was partitioned into four 25-item packets for fresh-context execution. No item has been promoted without a valid second-pass result.
- Added the v0.3 review runbook with exact packet, validation, reconciliation, acceptance, and release-audit commands.
- Executed four schema-valid blinded protocol passes (25 items each), reconciled all 100 items, and recorded explicit adjudications.
- Promoted 70 bounded questions and explicitly excluded 30 front-matter-only prompts; review coverage is complete with no unresolved items.

Next permitted phase: use the accepted benchmark for release reporting; retain the excluded prompts as regeneration work rather than silently scoring them.

## Phase 8 — Retrieval evaluation and grounded-answer path

Status: complete

Evidence:

- Added a passage-anchor v0.3 evaluation command that excludes unanswerable questions from the retrieval denominator and records them as refusal-required cases.
- Added a deterministic fake model adapter and grounded-answer result contract with source metadata, citations, interpretation boundary, and uncertainty text.
- Added a source/question-sliced development report for the acquired-source pack.
- Candidate BM25 diagnostic: 90 answerable items, hit-rate-like diagnostic 0.2444; this is not a release score because candidates are not yet reviewed or accepted.

Next permitted phase: add reviewed benchmark controls and compare retrieval profiles only after candidate review/freeze.

## Phase 10 — Reporting and release audit

Status: complete

Evidence:

- Added a source/question-sliced v0.3 development report and a fail-closed release audit.
- The release audit is now green after Plotinus scope verification and benchmark acceptance.
- README documents the eight-source development pack and labels the 100-question artifact as candidates rather than validated results.
- Final regression gate: 45 tests pass; privacy/portability and `git diff --check` pass.
- Added a Plotinus acquisition note documenting the recovered original MacKenna candidates, the explicitly excluded B. S. Page revision, and the missing original-volume proof. Final regression gate now passes 46 tests.
- Added review-coverage auditing and the engineering/client-boundary case study; final regression gate now passes 48 tests.
- The accepted benchmark contains 70 accepted and 30 explicitly excluded items; the release audit treats exclusions as resolved outcomes.

Next permitted phase: publish a release note only if the owner is comfortable describing the protocol-assisted reviewer as consistency evidence rather than scholarly validation.

## Phase 9 — Grounded local ask experience and project wordmark

Status: complete

Evidence:

- `ask` provides local grounded evidence, optional deterministic fake generation, citations, uncertainty, and an explicit refusal path.
- Added the branded Local Scriptorium wordmark for README presentation; CLI output remains plain and machine-safe.
- CLI process tests and the full regression suite pass.
